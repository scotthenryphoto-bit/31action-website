# 31 ACTION Cloudflare starter

This folder is a deployment starter, not a live backend. Before deployment, configure owner/customer authentication, create the private R2 bucket and D1 database, apply `schema.sql`, bind both resources in Wrangler, and connect the admin page to authenticated API endpoints. Do not deploy the upload route publicly without authentication.
